name: Rotate logo

on:
  workflow_dispatch:

jobs:
  debug-test:
    runs-on: ubuntu-latest
    steps:
      - name: Print a unique debugging message
        run: |
          echo "--- HELLO WORLD TEST ---"
          echo "If you see this message in the log, the file update was successful."
          echo "Test run at: $(date)"
